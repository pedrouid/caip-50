import { CAIP10AccountParams, CAIP50Account } from './types';

export function decode(caip50: CAIP50Account): CAIP10AccountParams {
  // TODO: implement decode
  return { chainId: '', address: '' };
}
